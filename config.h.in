#ifndef __CONFIG_H
#define __CONFIG_H

#cmakedefine HAVE_FLOCK 1
#cmakedefine HAVE_XMLRPC 1

#cmakedefine USE_XMLRPC 1

/* settings */
#cmakedefine HTML_INDEX_STORIES		${HTML_INDEX_STORIES}
#cmakedefine HTML_ARCHIVE_STORIES	${HTML_ARCHIVE_STORIES}
#cmakedefine HTML_CATEGORY_STORIES	${HTML_CATEGORY_STORIES}
#cmakedefine HTML_TAG_STORIES		${HTML_TAG_STORIES}
#cmakedefine FEED_INDEX_STORIES		${FEED_INDEX_STORIES}

#cmakedefine COMMENT_MAX_DELAY		(${COMMENT_MAX_DELAY} * 1000000000ULL)
#cmakedefine COMMENT_MIN_DELAY		(${COMMENT_MIN_DELAY} * 1000000000ULL)

#cmakedefine DB_FILE			"${DB_FILE}"

#cmakedefine ERROR_LOG_FILE		"${ERROR_LOG_FILE}"

#cmakedefine BASE_URL			"${BASE_URL}"
#cmakedefine BUG_BASE_URL		"${BUG_BASE_URL}"
#cmakedefine WIKI_BASE_URL		"${WIKI_BASE_URL}"
#cmakedefine PHOTO_BASE_URL		"${PHOTO_BASE_URL}"

#cmakedefine VAR_MAX_SCOPES		${VAR_MAX_SCOPES}
#cmakedefine VAR_MAX_VAR_NAME		${VAR_MAX_VAR_NAME}
#cmakedefine VAR_MAX_ARRAY_SIZE		${VAR_MAX_ARRAY_SIZE}
#cmakedefine VAR_MAX_VARS_SIZE		${VAR_MAX_VARS_SIZE}

#cmakedefine TAGCLOUD_MIN_SIZE		${TAGCLOUD_MIN_SIZE}
#cmakedefine TAGCLOUD_MAX_SIZE		${TAGCLOUD_MAX_SIZE}

#cmakedefine LATEX_BIN			"${LATEX_BIN}"
#cmakedefine DVIPNG_BIN			"${DVIPNG_BIN}"

#cmakedefine PREVIEW_SECRET		${PREVIEW_SECRET}

#endif
